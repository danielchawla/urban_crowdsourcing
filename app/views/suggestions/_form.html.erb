<% if suggestion.errors.any? %>
<div id="error_explanation">
  <h4><%= pluralize(@suggestion.errors.count, " error") %> prohibited this suggestion from being saved:</h4>

  <ul>
    <% suggestion.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<!-- <%= form_for suggestion, :url => suggestions_path, :html => { :multipart => true } do |f| %>
  <%= f.file_field :sug_img %>
<% end %> -->


<%= form_for suggestion, :url => suggestions_path, :html => { :multipart => true } do |f| %>
  <%= f.label :overview, "Title" %>:<br/>
  <%= f.text_field :overview %><br />
 
  <%= f.label :description %>:<br/>
  <%= f.text_field :description %><br />

  <%= f.label :location , "Address"%>:<br/>
  <%= f.text_field :location %><br />
  <br/>
  <strong>Upload an Image (optional):</strong> <%= f.file_field :sug_img %>

  <br/>
  <%= f.submit%>
<% end %>

